---
phase: 03-ui-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webconfig/templates/components/wake-word-panel.html
  - webconfig/templates/index.html
  - webconfig/templates/base.html
  - webconfig/static/js/init.js
autonomous: true
requirements: [WWUI-01, WWUI-02, WWUI-03, WWUI-04, WWUI-05, WWUI-06, WWUI-07, WWUI-08, WWUI-09, WWUI-10]

must_haves:
  truths:
    - "Wake word panel appears on the dashboard after Audio Settings, before MQTT"
    - "Panel has collapsible header with hearing icon and 'Wake Word' title"
    - "Panel contains status badge, enable toggle, action buttons, event log, and calibration wizard HTML"
    - "wake-word-panel.js script tag loads before sections.js in base.html"
    - "WakeWordPanel.init() is called in init.js DOMContentLoaded handler"
  artifacts:
    - path: "webconfig/templates/components/wake-word-panel.html"
      provides: "Complete wake word panel HTML template"
      min_lines: 80
    - path: "webconfig/templates/index.html"
      provides: "Panel include after settings-panel"
      contains: "wake-word-panel.html"
    - path: "webconfig/templates/base.html"
      provides: "Script tag for wake-word-panel.js"
      contains: "wake-word-panel.js"
    - path: "webconfig/static/js/init.js"
      provides: "WakeWordPanel.init() call"
      contains: "WakeWordPanel.init"
  key_links:
    - from: "webconfig/templates/index.html"
      to: "webconfig/templates/components/wake-word-panel.html"
      via: "Jinja2 include"
      pattern: "include.*wake-word-panel"
    - from: "webconfig/templates/base.html"
      to: "webconfig/static/js/wake-word-panel.js"
      via: "script tag"
      pattern: "wake-word-panel.js"
---

<objective>
Create the wake word panel HTML template and wire it into the dashboard.

Purpose: Provides the complete DOM structure that the JS module (Plan 02) will bring to life. All element IDs, classes, and layout must match the UI-SPEC exactly so the JS can bind to them without modification.

Output: wake-word-panel.html template, updated index.html/base.html/init.js
</objective>

<execution_context>
@/home/pi/billy-b-assistant/.claude/get-shit-done/workflows/execute-plan.md
@/home/pi/billy-b-assistant/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-panel/03-UI-SPEC.md
@.planning/phases/03-ui-panel/03-CONTEXT.md
@.planning/phases/03-ui-panel/03-RESEARCH.md
@.planning/phases/02-web-routes-settings/02-02-SUMMARY.md

<interfaces>
<!-- Existing DOM patterns the template must match -->

From webconfig/templates/components/settings-form.html (collapsible section pattern):
```html
<div class="bg-zinc-900/50 backdrop-blur-xs border border-zinc-700 p-4 rounded-lg shadow-lg mb-4 collapsible-section"
     id="section-{name}">
    <h3 class="flex items-center gap-2 cursor-pointer group text-lg grow justify-between font-semibold mb-4 text-slate-200 hover:text-emerald-400 transition-colors">
        <span class="material-icons text-white group-hover:text-emerald-400 transition-colors">{icon}</span>
        {Title}
        <span class="material-icons transition-transform duration-200 ml-2 rotate-0 group-hover:text-emerald-400">expand_more</span>
    </h3>
    <!-- children toggled by Sections.collapsible() -->
</div>
```

From webconfig/static/js/sections.js (auto-initialization):
- Sections.collapsible() scans all `.collapsible-section` elements
- Uses `localStorage.getItem('collapse_' + section.id)` for persistence
- Toggles h3 sibling children hidden/visible
- Uses `data-collapsible-initialized` attribute to prevent re-init

From webconfig/templates/index.html (current include order):
```html
{% include "components/settings-panel.html" %}
{% include "components/env-editor-modal.html" %}
```

From webconfig/templates/base.html (current script order before sections.js):
```html
<script src="{{ url_for('static', filename='js/audio-panel.js') }}"></script>
<script src="{{ url_for('static', filename='js/motor-panel.js') }}"></script>
<script src="{{ url_for('static', filename='js/sections.js') }}"></script>
```

From webconfig/static/js/init.js (DOMContentLoaded handler):
```javascript
document.addEventListener("DOMContentLoaded", async () => {
    // ... existing init calls ...
    Sections.collapsible();
    ReleaseNotes.init();
    SongsManager.init();
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wake-word-panel.html template with complete panel structure</name>
  <files>webconfig/templates/components/wake-word-panel.html</files>
  <read_first>
    webconfig/templates/components/settings-form.html
    .planning/phases/03-ui-panel/03-UI-SPEC.md
    .planning/phases/03-ui-panel/03-CONTEXT.md
  </read_first>
  <action>
Create the file `webconfig/templates/components/wake-word-panel.html` with the complete wake word panel HTML. Every element ID, class, and structure must match the UI-SPEC exactly. The panel is NOT inside the settings form -- it is a standalone dashboard section.

**Panel container (per D-07, D-09):**
```html
<section id="wake-word-panel" class="max-w-6xl mx-auto mt-2 px-2 md:px-0">
    <div class="bg-zinc-900/50 backdrop-blur-xs border border-zinc-700 p-4 rounded-lg shadow-lg mb-4 collapsible-section"
         id="section-wake-word-panel">
```
CRITICAL: The section ID MUST be `section-wake-word-panel` (NOT `section-wake-word`) to avoid localStorage collision with the settings-form wake word section.

**Section heading:**
Use `font-semibold` (NOT `font-bold`) per UI-SPEC typography normalization. Icon: `hearing`. Title: `Wake Word`.

**Status Header Row (WWUI-01):**
Contains status badge (id=`ww-status-badge`, dot id=`ww-status-dot`, text id=`ww-status-text`) and enable/disable toggle (id=`ww-enable-toggle`). Default badge state: emerald/Listening. Toggle uses sr-only input + styled div.

Exact badge HTML:
```html
<div id="ww-status-badge" class="flex items-center gap-2 px-3 py-1.5 rounded-full bg-emerald-500/20">
    <span id="ww-status-dot" class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span>
    <span id="ww-status-text" class="text-xs font-semibold text-emerald-400">Listening</span>
</div>
```

Exact toggle HTML:
```html
<label class="flex items-center gap-2 cursor-pointer">
    <span class="text-sm text-slate-300">Enable</span>
    <div class="relative">
        <input type="checkbox" id="ww-enable-toggle" class="sr-only peer">
        <div class="w-10 h-5 bg-zinc-700 rounded-full peer peer-checked:bg-emerald-500 transition-colors"></div>
        <div class="absolute left-0.5 top-0.5 w-4 h-4 bg-white rounded-full transition-transform peer-checked:translate-x-5"></div>
    </div>
</label>
```

**Action Buttons Row (WWUI-04):**
Three buttons: Simulate Detection (id=`ww-simulate-btn`, icon=`play_arrow`), Stop Session (id=`ww-stop-btn`, icon=`stop`), Refresh Status (id=`ww-refresh-btn`, icon=`refresh`). All use `bg-zinc-800 hover:bg-zinc-700 text-white text-sm px-3 py-2 rounded transition-colors`.

**Event Log (WWUI-05):**
Header with "Event Log" (h4, text-sm font-semibold text-slate-300) and event count (id=`ww-event-count`, text-xs text-slate-400, initial "0 events"). Container (id=`ww-event-log`) with `bg-zinc-800 rounded border border-zinc-700 max-h-48 overflow-y-auto text-sm font-mono`. Empty state div (id=`ww-event-empty`) with placeholder text: "No detection events yet. Events appear here when the wake word is detected."

**Calibration Wizard (WWUI-06, WWUI-07, WWUI-08):**
Step indicators: three steps with circle + label. Step 1 active (emerald), steps 2-3 inactive (zinc). Lines between steps: `flex-1 h-px bg-zinc-700`.

Step indicator IDs: `ww-cal-step-1`, `ww-cal-step-2`, `ww-cal-step-3`.

Content area (id=`ww-cal-content`) with `bg-zinc-800 rounded border border-zinc-700 p-4`.

Step 1 content (id=`ww-cal-step1-content`): Description text "Measure your environment's background noise level. Keep the room quiet and press Start." + "Measure Background" button (id=`ww-cal-ambient-btn`, icon=`graphic_eq`).

Step 2 content (id=`ww-cal-step2-content`, initially hidden): Description "Say your wake word clearly. Press Start and speak normally." + "Record Wake Phrase" button (id=`ww-cal-phrase-btn`, icon=`graphic_eq`).

Step 3 content (id=`ww-cal-step3-content`, initially hidden): Suggested threshold value (id=`ww-cal-suggested-value`), persist checkbox (id=`ww-cal-persist`, checked by default per D-05), "Apply Suggestions" button (id=`ww-cal-apply-btn`, `bg-emerald-500 hover:bg-emerald-400 text-zinc-800 font-semibold`).

Results grid area (id=`ww-cal-results`, initially hidden): 2x2 grid with metric cards. Metric value IDs: `ww-cal-ambient-rms`, `ww-cal-peak-rms`, `ww-cal-suggested-threshold`, `ww-cal-phrase-peak`.

Recording state area (id=`ww-cal-recording`, initially hidden): Pulsing red dot + status text (id=`ww-cal-status`) + countdown (id=`ww-cal-countdown`).

Note on WWUI-02 and WWUI-03: These requirements (Porcupine endpoint/access key inputs, sensitivity slider/threshold input) are ALREADY SATISFIED by Phase 2's settings-form.html additions. The dashboard panel does NOT duplicate these fields. The panel provides runtime threshold adjustment via the calibration wizard (WWUI-06/07/08).
  </action>
  <verify>
    <automated>test -f webconfig/templates/components/wake-word-panel.html && grep -c "ww-status-badge\|ww-enable-toggle\|ww-simulate-btn\|ww-stop-btn\|ww-refresh-btn\|ww-event-log\|ww-cal-content\|ww-cal-apply-btn" webconfig/templates/components/wake-word-panel.html | grep -q "^8$" && echo "PASS: All 8 key element IDs found" || echo "FAIL: Missing element IDs"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at webconfig/templates/components/wake-word-panel.html
    - grep "section-wake-word-panel" finds the collapsible section ID (NOT section-wake-word)
    - grep "ww-status-badge" finds the status badge container
    - grep "ww-enable-toggle" finds the enable/disable checkbox
    - grep "ww-simulate-btn" finds the simulate button
    - grep "ww-stop-btn" finds the stop button
    - grep "ww-refresh-btn" finds the refresh button
    - grep "ww-event-log" finds the event log container
    - grep "ww-event-empty" finds the empty state placeholder
    - grep "ww-cal-content" finds the calibration content area
    - grep "ww-cal-step1-content" finds step 1
    - grep "ww-cal-step2-content" finds step 2
    - grep "ww-cal-step3-content" finds step 3
    - grep "ww-cal-apply-btn" finds the apply button
    - grep "ww-cal-persist" finds the persist checkbox with checked attribute
    - grep "font-semibold" used for heading weight (NOT font-bold)
    - grep "hearing" finds the Material Icons icon name
  </acceptance_criteria>
  <done>Complete wake-word-panel.html exists with all UI-SPEC element IDs, correct section ID, collapsible structure, and all five sections (status header, action buttons, event log, calibration wizard, results grid)</done>
</task>

<task type="auto">
  <name>Task 2: Wire panel template into dashboard (index.html, base.html, init.js)</name>
  <files>webconfig/templates/index.html, webconfig/templates/base.html, webconfig/static/js/init.js</files>
  <read_first>
    webconfig/templates/index.html
    webconfig/templates/base.html
    webconfig/static/js/init.js
  </read_first>
  <action>
Three targeted modifications to wire the new panel into the dashboard:

**1. index.html (per D-08: after Audio Settings/settings-panel, before MQTT/modals):**
Insert `{% include "components/wake-word-panel.html" %}` between the settings-panel include and the env-editor-modal include. The result should be:
```html
<!-- Settings Panel -->
{% include "components/settings-panel.html" %}

<!-- Wake Word Panel -->
{% include "components/wake-word-panel.html" %}

{% include "components/env-editor-modal.html" %}
```

**2. base.html (per UI-SPEC script load order):**
Add the wake-word-panel.js script tag BEFORE sections.js. Find the line:
```html
<script src="{{ url_for('static', filename='js/sections.js') }}"></script>
```
Insert BEFORE it:
```html
<script src="{{ url_for('static', filename='js/wake-word-panel.js') }}"></script>
```

**3. init.js (WWUI-10: init on DOMContentLoaded):**
Add `WakeWordPanel.init();` inside the DOMContentLoaded handler. Place it BEFORE `Sections.collapsible();` so the panel's internal state is ready before collapsible sections are initialized. Add a typeof guard matching the existing AudioPanel pattern:
```javascript
if (typeof WakeWordPanel !== 'undefined') {
    WakeWordPanel.init();
}
```
Place this right before the `Sections.collapsible();` call (around line 77).
  </action>
  <verify>
    <automated>grep -q "wake-word-panel.html" webconfig/templates/index.html && grep -q "wake-word-panel.js" webconfig/templates/base.html && grep -q "WakeWordPanel.init" webconfig/static/js/init.js && echo "PASS: All three wiring points connected" || echo "FAIL: Missing wiring"</automated>
  </verify>
  <acceptance_criteria>
    - grep "wake-word-panel.html" webconfig/templates/index.html returns the include line
    - grep "wake-word-panel.js" webconfig/templates/base.html returns the script tag
    - grep "WakeWordPanel.init" webconfig/static/js/init.js returns the init call
    - In index.html: the wake-word-panel include appears AFTER settings-panel and BEFORE env-editor-modal
    - In base.html: wake-word-panel.js script tag appears BEFORE sections.js script tag
    - In init.js: WakeWordPanel.init() call appears BEFORE Sections.collapsible() call
  </acceptance_criteria>
  <done>Dashboard loads the wake word panel template, the JS file loads before sections.js, and WakeWordPanel.init() is called during DOMContentLoaded initialization</done>
</task>

</tasks>

<verification>
1. All 8 key DOM element IDs exist in wake-word-panel.html: ww-status-badge, ww-enable-toggle, ww-simulate-btn, ww-stop-btn, ww-refresh-btn, ww-event-log, ww-cal-content, ww-cal-apply-btn
2. Section ID is section-wake-word-panel (no collision with settings-form section-wake-word)
3. index.html includes wake-word-panel.html after settings-panel.html
4. base.html loads wake-word-panel.js before sections.js
5. init.js calls WakeWordPanel.init() before Sections.collapsible()
</verification>

<success_criteria>
- The dashboard loads without JS errors (the template renders, the script tag exists even though wake-word-panel.js doesn't exist yet)
- All DOM elements the JS module (Plan 02) will bind to are present with correct IDs
- Collapsible section initializes correctly via Sections.collapsible() with no ID conflicts
- Panel is positioned correctly in the page layout (after settings, before modals)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-panel/03-01-SUMMARY.md`
</output>
