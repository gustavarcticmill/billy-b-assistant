---
phase: 02-web-routes-settings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - webconfig/app/routes/system.py
  - webconfig/templates/components/settings-form.html
autonomous: true
requirements:
  - SETS-01
  - SETS-02

must_haves:
  truths:
    - "Wake word config keys are saved to .env when the main settings form is submitted"
    - "Wake word settings section appears in the settings form between Audio Settings and MQTT Settings"
    - "Form field names exactly match the CONFIG_KEYS entries and .env variable names"
  artifacts:
    - path: "webconfig/app/routes/system.py"
      provides: "5 wake word keys in CONFIG_KEYS list"
      contains: "WAKE_WORD_ENABLED"
    - path: "webconfig/templates/components/settings-form.html"
      provides: "Wake Word Settings collapsible section with 5 form fields"
      contains: "section-wake-word"
  key_links:
    - from: "webconfig/templates/components/settings-form.html"
      to: "webconfig/app/routes/system.py"
      via: "form field name attributes matching CONFIG_KEYS entries"
      pattern: "WAKE_WORD_ENABLED|WAKE_WORD_SENSITIVITY|WAKE_WORD_THRESHOLD"
---

<objective>
Add wake word configuration keys to the settings persistence flow and add the settings form section to the HTML template.

Purpose: Enables wake word settings (enable/disable, sensitivity, threshold, endpoint, access key) to be configured and saved via the existing web settings form. This is the persistence layer that ensures settings survive reboots (SETS-01) and integrates into the existing save mechanism (SETS-02).

Output: Updated `system.py` CONFIG_KEYS, new Wake Word Settings section in `settings-form.html`.
</objective>

<execution_context>
@/home/pi/billy-b-assistant/.claude/get-shit-done/workflows/execute-plan.md
@/home/pi/billy-b-assistant/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-web-routes-settings/02-CONTEXT.md
@.planning/phases/02-web-routes-settings/02-RESEARCH.md
@.planning/phases/02-web-routes-settings/02-UI-SPEC.md

<interfaces>
<!-- Key contracts from existing codebase -->

From webconfig/app/routes/system.py (CONFIG_KEYS, lines 40-68):
```python
CONFIG_KEYS = [
    "OPENAI_API_KEY",
    "OPENAI_MODEL",
    # ... 25 more keys ...
    "NEWS_REQUEST_TIMEOUT_SECONDS",
]
```
The save route iterates CONFIG_KEYS and writes each matching key to .env via set_key().

From core/config.py (wake word env var names, lines 276-284):
```python
WAKE_WORD_ENABLED = os.getenv("WAKE_WORD_ENABLED", "false").lower() == "true"
WAKE_WORD_SENSITIVITY = _float_env_ranged("WAKE_WORD_SENSITIVITY", "0.5", min_val=0.0, max_val=1.0)
WAKE_WORD_THRESHOLD = _float_env_ranged("WAKE_WORD_THRESHOLD", "2400", min_val=0.0)
WAKE_WORD_ENDPOINT = os.getenv("WAKE_WORD_ENDPOINT", "").strip()
WAKE_WORD_PORCUPINE_ACCESS_KEY = (
    os.getenv("WAKE_WORD_PORCUPINE_ACCESS_KEY")
    or os.getenv("PICOVOICE_ACCESS_KEY", "")
).strip()
```

From webconfig/templates/components/settings-form.html:
- section-audio ends at line 418 (closing </div>)
- section-mqtt starts at line 420-422
- Insertion point for wake word section: between line 418 and line 420
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wake word keys to CONFIG_KEYS in system.py</name>
  <files>webconfig/app/routes/system.py</files>
  <read_first>
    webconfig/app/routes/system.py
    core/config.py
  </read_first>
  <action>
Edit `webconfig/app/routes/system.py` to add 5 wake word configuration keys to the CONFIG_KEYS list (SETS-01, SETS-02, per D-06).

Add these 5 entries at the end of the CONFIG_KEYS list (before the closing `]`), after `"NEWS_REQUEST_TIMEOUT_SECONDS"`:

```python
    "WAKE_WORD_ENABLED",
    "WAKE_WORD_SENSITIVITY",
    "WAKE_WORD_THRESHOLD",
    "WAKE_WORD_ENDPOINT",
    "WAKE_WORD_PORCUPINE_ACCESS_KEY",
```

The exact env var names MUST match what `core/config.py` reads (per Pitfall 3 in RESEARCH.md):
- `WAKE_WORD_ENABLED` (not just `ENABLED`)
- `WAKE_WORD_SENSITIVITY` (not just `SENSITIVITY`)
- `WAKE_WORD_THRESHOLD` (not just `THRESHOLD`)
- `WAKE_WORD_ENDPOINT` (not just `ENDPOINT`)
- `WAKE_WORD_PORCUPINE_ACCESS_KEY` (not just `PORCUPINE_ACCESS_KEY`)

Do NOT add a trailing comma after the last entry if none exists in the current list style. Match the existing formatting.
  </action>
  <verify>
    <automated>cd /home/pi/billy-b-assistant && grep -c "WAKE_WORD_" webconfig/app/routes/system.py</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms: `"WAKE_WORD_ENABLED"` in system.py CONFIG_KEYS
    - grep confirms: `"WAKE_WORD_SENSITIVITY"` in system.py CONFIG_KEYS
    - grep confirms: `"WAKE_WORD_THRESHOLD"` in system.py CONFIG_KEYS
    - grep confirms: `"WAKE_WORD_ENDPOINT"` in system.py CONFIG_KEYS
    - grep confirms: `"WAKE_WORD_PORCUPINE_ACCESS_KEY"` in system.py CONFIG_KEYS
    - Total of 5 WAKE_WORD_ entries added (grep -c returns 5)
  </acceptance_criteria>
  <done>CONFIG_KEYS list contains all 5 wake word keys. The existing save endpoint will now persist these values to .env when submitted.</done>
</task>

<task type="auto">
  <name>Task 2: Add Wake Word Settings section to settings form template</name>
  <files>webconfig/templates/components/settings-form.html</files>
  <read_first>
    webconfig/templates/components/settings-form.html
    .planning/phases/02-web-routes-settings/02-UI-SPEC.md
  </read_first>
  <action>
Edit `webconfig/templates/components/settings-form.html` to insert a new "Wake Word Settings" collapsible section between the Audio Settings section (ends with `</div>` at approximately line 418) and the MQTT Settings section (starts at approximately line 420).

Insert the following HTML block. All patterns match existing sections exactly (per UI-SPEC.md):

```html
        <!-- Wake Word Settings -->
        <div class="bg-zinc-900/50 backdrop-blur-xs border border-zinc-700 p-4 rounded-lg shadow-lg mb-4 collapsible-section"
             id="section-wake-word">
            <h3 class="flex items-center gap-2 cursor-pointer group text-lg grow justify-between font-bold mb-4 text-slate-200 hover:text-emerald-400 transition-colors">
                <span class="material-icons text-white group-hover:text-emerald-400 transition-colors">hearing</span>
                Wake Word Settings
                <span class="material-icons transition-transform duration-200 ml-2 rotate-0 group-hover:text-emerald-400">expand_more</span>
            </h3>

            <!-- Enable/Disable Toggle (full width select) -->
            <div class="mb-4">
                <label for="WAKE_WORD_ENABLED"
                       class="flex justify-between items-center font-bold text-sm text-slate-300 relative">
                    Enable Wake Word Detection
                    <span class="material-icons align-middle hover:text-cyan-400 cursor-pointer ml-1"
                          onclick="toggleTooltip(this)">help_outline</span>
                </label>
                <div class="relative">
                    <div data-tooltip>
                        When enabled, Billy listens for the wake word and starts a conversation
                        session hands-free. Disable to use button-only mode.
                    </div>
                    <select id="WAKE_WORD_ENABLED" name="WAKE_WORD_ENABLED"
                            class="w-full p-3 mt-1 bg-zinc-800 text-white rounded focus:outline-none focus:ring-2 focus:ring-cyan-500">
                        <option value="false" {{ 'selected' if config.get('WAKE_WORD_ENABLED', 'false') == 'false' else '' }}>Disabled</option>
                        <option value="true" {{ 'selected' if config.get('WAKE_WORD_ENABLED', 'false') == 'true' else '' }}>Enabled</option>
                    </select>
                </div>
            </div>

            <!-- Sensitivity + RMS Threshold (side by side, matches Mic Timeout + Silence Threshold pattern) -->
            <div class="flex mb-4 gap-4">
                <div class="flex-1/2 flex flex-col justify-between">
                    <label for="WAKE_WORD_SENSITIVITY"
                           class="flex justify-between items-center font-bold text-sm text-slate-300">
                        Sensitivity (0.0 - 1.0)
                        <span class="material-icons align-middle hover:text-cyan-400 cursor-pointer ml-1"
                              onclick="toggleTooltip(this)">help_outline</span>
                    </label>
                    <div class="relative">
                        <div data-tooltip>
                            How sensitive the wake word engine is to detections. Higher values
                            catch more utterances but may produce false positives. Default: 0.5
                        </div>
                        <input type="number" step="0.05" min="0.0" max="1.0"
                               id="WAKE_WORD_SENSITIVITY" name="WAKE_WORD_SENSITIVITY"
                               class="w-full p-3 mt-1 bg-zinc-800 text-white rounded focus:outline-none focus:ring-2 focus:ring-cyan-500"
                               value="{{ config.get('WAKE_WORD_SENSITIVITY', '0.5') }}">
                    </div>
                </div>

                <div class="flex-1/2 flex flex-col justify-between">
                    <label for="WAKE_WORD_THRESHOLD"
                           class="flex justify-between items-center font-bold text-sm text-slate-300">
                        RMS Threshold (0 - 32768)
                        <span class="material-icons align-middle hover:text-cyan-400 cursor-pointer ml-1"
                              onclick="toggleTooltip(this)">help_outline</span>
                    </label>
                    <div class="relative">
                        <div data-tooltip>
                            Minimum audio RMS level to consider as speech. Use the calibration
                            tool in the Wake Word panel to find the right value for your
                            environment. Default: 1000
                        </div>
                        <input type="number" step="1" min="0" max="32768"
                               id="WAKE_WORD_THRESHOLD" name="WAKE_WORD_THRESHOLD"
                               class="w-full p-3 mt-1 bg-zinc-800 text-white rounded focus:outline-none focus:ring-2 focus:ring-cyan-500"
                               value="{{ config.get('WAKE_WORD_THRESHOLD', '2400') }}">
                    </div>
                </div>
            </div>

            <!-- Endpoint Path (full width) -->
            <div class="mb-4">
                <label for="WAKE_WORD_ENDPOINT"
                       class="flex justify-between items-center font-bold text-sm text-slate-300 relative">
                    Porcupine Endpoint Path
                    <span class="material-icons align-middle hover:text-cyan-400 cursor-pointer ml-1"
                          onclick="toggleTooltip(this)">help_outline</span>
                </label>
                <div class="relative">
                    <div data-tooltip>
                        Path to a custom Porcupine .ppn endpoint file. Leave empty to use the
                        built-in "Porcupine" keyword.
                    </div>
                    <input id="WAKE_WORD_ENDPOINT" name="WAKE_WORD_ENDPOINT" type="text"
                           class="w-full p-3 mt-1 bg-zinc-800 text-white rounded focus:outline-none focus:ring-2 focus:ring-cyan-500"
                           value="{{ config.get('WAKE_WORD_ENDPOINT', '') }}">
                </div>
            </div>

            <!-- Access Key (password field with visibility toggle) -->
            <div class="mb-4">
                <label for="WAKE_WORD_PORCUPINE_ACCESS_KEY"
                       class="flex justify-between items-center font-bold text-sm text-slate-300 relative">
                    Porcupine Access Key
                    <span class="material-icons align-middle hover:text-cyan-400 cursor-pointer ml-1"
                          onclick="toggleTooltip(this)">help_outline</span>
                </label>
                <div class="relative">
                    <div data-tooltip>
                        Your Picovoice Console access key. Required for Porcupine wake word
                        detection. Get one free at
                        <a href="https://console.picovoice.ai/"
                           class="underline hover:text-cyan-400" target="_blank">console.picovoice.ai</a>
                    </div>
                    <input id="WAKE_WORD_PORCUPINE_ACCESS_KEY" name="WAKE_WORD_PORCUPINE_ACCESS_KEY"
                           type="password"
                           class="w-full p-3 mt-1 pr-10 bg-zinc-800 text-white rounded focus:outline-none focus:ring-2 focus:ring-cyan-500"
                           value="{{ config.get('WAKE_WORD_PORCUPINE_ACCESS_KEY', '') }}">
                    <button type="button"
                            onclick="toggleInputVisibility('WAKE_WORD_PORCUPINE_ACCESS_KEY', this)"
                            aria-label="Show password"
                            class="absolute inset-y-0 right-0 flex items-center px-2 text-slate-400 hover:text-white cursor-pointer">
                        <span class="material-icons" id="WAKE_WORD_PORCUPINE_ACCESS_KEY_icon">visibility</span>
                    </button>
                </div>
            </div>

        </div>
```

**Key details (per UI-SPEC.md):**
- Section icon: `hearing` (Material Icons)
- Section id: `section-wake-word`
- Enable toggle is a `<select>` with `true`/`false` options (per form inventory)
- Sensitivity + Threshold are side-by-side in `flex gap-4` row (matches Audio Settings pattern)
- Endpoint is a full-width text input
- Access key is a password field with visibility toggle using existing `toggleInputVisibility()` function
- All `name` attributes match CONFIG_KEYS entries exactly (per Pitfall 3)
- Default values match core/config.py defaults: WAKE_WORD_ENABLED=false, WAKE_WORD_SENSITIVITY=0.5, WAKE_WORD_THRESHOLD=2400, WAKE_WORD_ENDPOINT=empty, WAKE_WORD_PORCUPINE_ACCESS_KEY=empty
- Labels use `font-bold` per UI-SPEC typography (note: existing labels use `font-semibold` — use `font-bold` for the new section per UI-SPEC decision)
- All tooltips use the `data-tooltip` and `toggleTooltip(this)` pattern
  </action>
  <verify>
    <automated>cd /home/pi/billy-b-assistant && grep -c "WAKE_WORD_" webconfig/templates/components/settings-form.html</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms: `id="section-wake-word"` exists in settings-form.html
    - grep confirms: `name="WAKE_WORD_ENABLED"` exists in settings-form.html
    - grep confirms: `name="WAKE_WORD_SENSITIVITY"` exists in settings-form.html
    - grep confirms: `name="WAKE_WORD_THRESHOLD"` exists in settings-form.html
    - grep confirms: `name="WAKE_WORD_ENDPOINT"` exists in settings-form.html
    - grep confirms: `name="WAKE_WORD_PORCUPINE_ACCESS_KEY"` exists in settings-form.html
    - grep confirms: `hearing` icon present (section icon)
    - grep confirms: `Wake Word Settings` heading text present
    - grep confirms: `toggleInputVisibility` called for access key field
    - grep confirms: section appears BEFORE `section-mqtt` in the file (section-wake-word line number < section-mqtt line number)
  </acceptance_criteria>
  <done>Wake Word Settings section exists in settings-form.html between Audio Settings and MQTT Settings. All 5 form fields have correct name attributes matching CONFIG_KEYS. Section follows existing collapsible pattern with tooltips.</done>
</task>

</tasks>

<verification>
1. `grep -c "WAKE_WORD_" webconfig/app/routes/system.py` returns 5
2. `grep "section-wake-word" webconfig/templates/components/settings-form.html` finds the section
3. All form field `name` attributes match CONFIG_KEYS entries exactly
4. Section is positioned between section-audio and section-mqtt in the template
5. No changes to core/config.py or core/hotword.py
</verification>

<success_criteria>
- 5 wake word keys added to CONFIG_KEYS in system.py (SETS-01, SETS-02)
- Wake Word Settings section added to settings-form.html between Audio and MQTT sections
- All form field names match CONFIG_KEYS entries exactly (WAKE_WORD_ENABLED, WAKE_WORD_SENSITIVITY, WAKE_WORD_THRESHOLD, WAKE_WORD_ENDPOINT, WAKE_WORD_PORCUPINE_ACCESS_KEY)
- Section uses correct icon (hearing), heading (Wake Word Settings), and all copywriting per UI-SPEC
- Enable toggle is a select element, sensitivity/threshold side-by-side, endpoint text, access key password with toggle
</success_criteria>

<output>
After completion, create `.planning/phases/02-web-routes-settings/02-02-SUMMARY.md`
</output>
